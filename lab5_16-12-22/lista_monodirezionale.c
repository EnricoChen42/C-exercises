#include <stdio.h>
#include <stdlib.h>

typedef struct elem{
    char codice;
    int quantita;
    struct elem *next;  
} s_elem;

void aggiungi(s_elem *r);

int main()
{
    s_elem *r = (struct elem *) malloc(sizeof(struct elem));

     

     8op 
}

void aggiungi(s_elem *r)
{
    s_elem *s = (struct elem *) malloc(sizeof(struct elem));



}
